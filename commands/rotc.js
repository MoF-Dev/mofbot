module.exports = {
    name: 'rotc',
    description: 'Explains how to fire a mortar.',
    args: false,
    execute(message, args) {
        message.channel.send("The explosion is pushed in the shell, and the shell is pushed in the mortar. The mortar is then pushed. The shell fires because the shell; the shell fires. The shell then explodes, killing people because of exploding. Then they say gg because etai, but it was ebola.")
    },
};